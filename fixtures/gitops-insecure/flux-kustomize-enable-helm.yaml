apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmGlobals:
  chartHome: ../charts
  configHome: /tmp
helmCharts:
  - name: remote-chart
    repo: https://untrusted.example/charts
    version: ">1.0.0"
    releaseName: app
buildMetadata:
  - originAnnotations
# rendered with: kustomize build --enable-helm
