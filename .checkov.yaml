# Checkov configuration for security-mcp CI
# The fixtures/ tree contains intentionally vulnerable Terraform, Kubernetes, and
# CloudFormation used as test input for the security gate engine (aws-insecure,
# azure-insecure, gcp-insecure, iac-insecure, infra-insecure, data-platform-insecure,
# k8s-insecure, gitops-insecure, web-insecure, ...). Suppress findings from the whole
# tree so Checkov can still enforce checks on any real IaC added elsewhere in the repo.

skip-path:
  - fixtures
