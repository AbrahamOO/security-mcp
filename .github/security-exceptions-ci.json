{
  "version": "1.0.0",
  "_description": "Security exceptions for the CI self-check gate. This file suppresses false positives that arise when the security-mcp tool scans its own source code. It is NOT used for user project gate runs.",
  "exceptions": [
    {
      "id": "self-scan-control-evidence",
      "finding_ids": ["CONTROL_EVIDENCE_MISSING", "EVIDENCE_MAPPING_MISSING", "IR_PLAYBOOK_MISSING", "ARTIFACTS_NO_PENTEST_SIGNOFF", "ARTIFACTS_COMPLIANCE_GAP", "CI_NO_PERMISSIONS"],
      "control_ids": [
        "NIST_AC_ACCESS_CONTROL",
        "NIST_AU_AUDIT_LOGGING",
        "NIST_IR_INCIDENT_RESPONSE",
        "NIST_IA_IDENTIFICATION",
        "NIST_SC_TRANSMISSION",
        "SOC2_CC6_LOGICAL_ACCESS",
        "SOC2_CC7_SYSTEM_MONITORING",
        "MITRE_INITIAL_ACCESS",
        "MITRE_CREDENTIAL_ACCESS",
        "MITRE_EXFILTRATION",
        "MITRE_DEFENSE_EVASION",
        "MITRE_LATERAL_MOVEMENT",
        "MITRE_COMMAND_CONTROL",
        "MITRE_IMPACT",
        "ZERO_TRUST",
        "ZERO_TRUST_CONTINUOUS_VERIFY",
        "ZERO_TRUST_MICROSEG",
        "TLS_13",
        "SECRET_MANAGER_ONLY",
        "CSRF",
        "SSRF",
        "CSP_NO_INLINE",
        "SLSA_L3_PROVENANCE",
        "AI_BOUNDED_OUTPUTS",
        "AI_LLM02_OUTPUT_HANDLING",
        "AI_LLM04_MODEL_DOS",
        "AI_LLM07_PLUGIN_DESIGN",
        "AI_LLM08_EXCESSIVE_AGENCY"
      ],
      "justification": "security-mcp is a developer CLI security scanner tool, not an application server. Controls requiring auth middleware, MFA enforcement, audit logging infrastructure, egress controls, and network segmentation are not applicable to this tool's architecture. The tool itself enforces these controls in user project code — it does not implement them internally.",
      "ticket": "internal/control-evidence-self-scan",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-01-01"
    },
    {
      "id": "self-scan-provenance",
      "finding_ids": ["PROVENANCE_MISSING", "SBOM_MISSING", "SBOM_UNSIGNED"],
      "control_ids": ["SLSA_L3_PROVENANCE", "NIST_SR_SUPPLY_CHAIN", "SBOM_GENERATED"],
      "justification": "SLSA provenance is generated by GitHub Actions via 'npm publish --provenance' and is attached to the npm package registry entry. The tool does not produce .intoto.jsonl files in .mcp/attestations/ because provenance is managed at the npm publish layer, not at the repo layer.",
      "ticket": "internal/slsa-provenance-self-scan",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-01-01"
    },
    {
      "id": "self-scan-remediation-map",
      "finding_ids": [
        "GRAPHQL_INTROSPECTION_ENABLED", "GRAPHQL_NO_DEPTH_LIMIT", "GRAPHQL_NO_BATCH_LIMIT", "GRAPHQL_NO_DATALOADER",
        "GRAPHQL_INTROSPECTION_ALWAYS_ON", "GRAPHQL_NO_COMPLEXITY_LIMIT",
        "DB_TLS_DISABLED", "DB_SQL_INJECTION_RISK", "DB_ADMIN_CREDENTIALS", "DB_HARDCODED_PASSWORD",
        "CRYPTO_WEAK_HASH", "CRYPTO_WEAK_CIPHER", "CRYPTO_INSECURE_RANDOM", "CRYPTO_WEAK_JWT_ALGO",
        "CRYPTO_GCM_NONCE_REUSE_RISK", "CRYPTO_SHA_USED_FOR_PASSWORD", "CRYPTO_INSECURE_RANDOM_IDENTIFIER",
        "DLP_REQUEST_BODY_LOGGED", "DLP_STACK_TRACE_IN_RESPONSE", "DLP_SERVER_HEADER_DISCLOSURE",
        "WEB_IDOR_RISK", "WEB_LOG_PII", "WEB_SESSION_WEAK_CONFIG",
        "WEAK_CRYPTO_HASH", "HARDCODED_DB_URL_OR_API_KEY", "HARDCODED_IP_ADDRESS",
        "WEB_GRAPHQL_INTROSPECTION", "DB_NO_POOL_LIMITS", "CRYPTO_ECDSA_P256_PQC",
        "LOG_INJECTION", "SQL_INJECTION", "SQL_INJECTION_CONCAT",
        "TLS_REJECT_UNAUTHORIZED_DISABLED",
        "JWT_MISSING_EXPIRY", "JWT_ALG_NOT_LOCKED"
      ],
      "justification": "These findings come from the REMEDIATION_MAP in src/mcp/server.ts, which contains intentional 'before' example code patterns (e.g., sslmode=disable, md5 hashing, console.log(req.body), postgresql://user:pass@host) used to show developers how to fix vulnerabilities. The patterns appear in string literals as educational examples, not as actual application code. GRAPHQL_INTROSPECTION_ALWAYS_ON, GRAPHQL_NO_COMPLEXITY_LIMIT, CRYPTO_GCM_NONCE_REUSE_RISK, CRYPTO_SHA_USED_FOR_PASSWORD, CRYPTO_INSECURE_RANDOM_IDENTIFIER, TLS_REJECT_UNAUTHORIZED_DISABLED, JWT_MISSING_EXPIRY, and JWT_ALG_NOT_LOCKED also match REMEDIATION_MAP examples, security checklist strings in server.ts, and .mcp/baselines/ stored scan data — none are in live application code. SQL_INJECTION and SQL_INJECTION_CONCAT match the REMEDIATION_MAP 'before' SQL example. WEAK_CRYPTO_HASH and HARDCODED_DB_URL_OR_API_KEY are additional IDs generated by the new deep-scan checks for the same REMEDIATION_MAP false-positive class. HARDCODED_IP_ADDRESS matches 127.0.0.1/localhost literals in tests and defaults. security-mcp has no HTTP server, no user sessions, and no cookies — these checks do not apply to its architecture.",
      "ticket": "internal/remediation-map-false-positives",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-01-01"
    },
    {
      "id": "self-scan-dep-scorecard",
      "finding_ids": ["DEP_LOW_SCORECARD", "DEPENDENCY_CONFUSION_RISK"],
      "justification": "Runtime dependencies (execa, fast-glob, picomatch, zod) have low OpenSSF Scorecard scores due to minimal OSSF project infrastructure (no Scorecard workflow, branch protection). These are mature, widely-used packages with no known critical CVEs. Replacement would introduce more risk than retention. Risk accepted pending upstream scorecard improvements. DEPENDENCY_CONFUSION_RISK for @modelcontextprotocol/sdk is expected — this is a public npm package from Anthropic; no private registry override is needed.",
      "ticket": "internal/dep-scorecard-review",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-01-01"
    },
    {
      "id": "self-scan-scanner-toolchain",
      "finding_ids": ["SCANNER_TOOLCHAIN_INCOMPLETE"],
      "justification": "The CI self-check environment has an empty scanner config (.github/security-tools-ci.json) because external scanners (gitleaks, semgrep, osv-scanner) are not installed in the GitHub Actions runner for this self-check step. The tool's own built-in checks cover the equivalent detections. Users are expected to have these tools installed in their own environments.",
      "ticket": "internal/scanner-toolchain-ci-self-scan",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-01-01"
    },
    {
      "id": "self-scan-doc-and-data-false-positives",
      "finding_ids": [
        "WEB_HEADERS_MISSING",
        "DANGEROUSLY_SET_INNER_HTML",
        "SSRF_GUARD_REQUIRED",
        "SSRF_USER_URL",
        "IAM_OVERPRIVILEGED",
        "PUBLIC_EXPOSURE_RISK",
        "JWT_ALG_NONE_ACCEPTED"
      ],
      "justification": "False positives from patterns matched in non-application files: DANGEROUSLY_SET_INNER_HTML matched in .mcp/baselines/ JSON data (a stored scan result, not code); SSRF_GUARD_REQUIRED matched 'undici-types' in package-lock.json (a type-definition package, not an HTTP client usage); IAM_OVERPRIVILEGED matched 'Action: *' in README.md documentation showing an anti-pattern; PUBLIC_EXPOSURE_RISK matched '0.0.0.0/0' in README.md explaining what to avoid; JWT_ALG_NONE_ACCEPTED matched a checklist item in server.ts that documents JWT security requirements as a string literal, not actual jwt.verify() code; WEB_HEADERS_MISSING and SSRF_USER_URL matched HTTP calls in src/cli/ that have explicit hostname allowlist checks (ALLOWED_SKILL_HOSTS, ALLOWED_BINARY_HOSTS) validating URLs before every fetch — all callers pass only hardcoded constants or pre-allowlisted URLs. security-mcp is a CLI/MCP scanner tool with no HTTP server surface, no JWT auth, and no IAM policies.",
      "ticket": "internal/doc-data-false-positives",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-01-01"
    },
    {
      "id": "self-scan-cli-and-repo-search",
      "finding_ids": [
        "COMMAND_INJECTION",
        "REDOS_USER_REGEXP",
        "REQUIRE_NON_LITERAL",
        "ARTIFACTS_NO_THREAT_MODEL",
        "ARTIFACTS_NO_SBOM",
        "POSSIBLE_SECRET",
        "GIT_HISTORY_SECRET",
        "CI_EXCEPTIONS_IN_LOCAL_SCAN",
        "AUDIT_LOGGING_DISABLED",
        "SECRET_DIST_NOT_SCANNED",
        "GRAPHQL_CIRCULAR_FRAGMENT_RISK",
        "BASELINE_REGRESSION"
      ],
      "control_ids": [],
      "justification": "COMMAND_INJECTION in src/cli/onboarding.ts and src/cli/update.ts — spawnSync/execSync are called with fixed command arrays derived from constants and an internal command builder, never from user-supplied strings. REDOS_USER_REGEXP in src/repo/search.ts — new RegExp() is intentional: the call is the final step of compileUserRegex() which enforces a 256-char limit, a nested-quantifier detection check, and lets Node.js throw SyntaxError for invalid patterns; the CWE-1333 risk is mitigated by these guards at the call site. REQUIRE_NON_LITERAL in src/cli/index.ts — require(resolve(__dirname, '../../package.json')) uses __dirname which is a compile-time constant, not user input; this pattern reads the bundled package.json for the version string. ARTIFACTS_NO_THREAT_MODEL and ARTIFACTS_NO_SBOM are project-level artifact gaps, not code-level vulnerabilities; they are tracked in CI (not pre-commit) and are covered by the self-scan-provenance and self-scan-control-evidence exceptions for the CI gate. POSSIBLE_SECRET in src/ci/pr-gate.ts matches process.env.SECURITY_GATE_POLICY with a file-path default — the default value is a config path, not a secret. GIT_HISTORY_SECRET matches JWT keyword in skills/stride-pasta-analyst/SKILL.md documentation and generic-api-key pattern in server.ts REMEDIATION_MAP — neither are actual secrets. CI_EXCEPTIONS_IN_LOCAL_SCAN is a meta-finding triggered when CI exception files are applied in local scans; intentional for the self-scan workflow where CI exceptions are the canonical exception set. AUDIT_LOGGING_DISABLED matched text in .mcp/baselines/ stored scan data, not actual application config. SECRET_DIST_NOT_SCANNED is expected — dist/ is generated from TypeScript source that is scanned; no secrets are introduced at build time. GRAPHQL_CIRCULAR_FRAGMENT_RISK is a new detection for circular GraphQL fragments; security-mcp has no GraphQL server. BASELINE_REGRESSION for PCI_REQ6_SECURE_SYSTEMS is a false positive in file_by_file pre-commit mode — that control depends on osv-scanner/trivy tooling that is not in scope for staged-file scanning; the control is satisfied in full CI runs.",
      "ticket": "internal/cli-repo-search-false-positives",
      "owner": "security-mcp-team",
      "approver": "ciso-review-board",
      "approval_role": "SecurityLead",
      "expires_on": "2027-06-07"
    }
  ]
}
